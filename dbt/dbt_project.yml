name: epi_pipeline
version: '1.0.0'
config-version: 2

profile: epi_pipeline

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
packages-install-path: "dbt_packages"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  epi_pipeline:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
